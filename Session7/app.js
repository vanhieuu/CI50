// firebase.firestore().collection("Todolist").add({Task:'2',Work:'Btvn'});
// firebase.firestore().collection("Todolist").add({Task:'3',Work:'Todolist'})
// thao tac crud vs firebase firestore
// thao tác với collection 
        // firebase.firestore().collection("tên collection")
        // Thêm 1 documents vao firestore
            
        // firebase.firestore().collection("tên collection").add({});
        
        // Sửa 1 document

        // Xóa 1 document

        //Lấy ra 1 document

        //collection -> document